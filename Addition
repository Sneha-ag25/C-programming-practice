#include <stdio.h>
#include <stdlib.h>
int main()
{
    int a, b, c;
    printf("enter first number");
    scanf("%d",&a);
    printf("enter second number");
    scanf("%d", &b);
    c=a+b;
    printf("the answer is: %d",c);
    
    system("pause"); 
    return 0;
}
